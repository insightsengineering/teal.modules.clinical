% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tm_t_ancova.R
\name{ancova}
\alias{ancova}
\alias{template_ancova}
\title{ANCOVA Teal Module}
\usage{
template_ancova(
  parentname,
  dataname,
  arm_var,
  ref_arm,
  cov_var,
  aval_var,
  avisit,
  paramcd,
  conf_level = 0.95
)
}
\arguments{
\item{parentname}{(\code{string})\cr parent analysis data used in teal module,
usually this refers to \code{"ADSL"}.}

\item{dataname}{(\code{string})\cr analysis data used in teal module.}

\item{arm_var}{(\code{string} or \code{choices_selected} with teal module)\cr
variable names designating the study arm.}

\item{ref_arm}{(\code{string})\cr the level of reference arm in case of
arm comparison.}

\item{cov_var}{(\code{string} or \code{choices_selected} with teal module)\cr
names of the variables for covariates.}

\item{aval_var}{(\code{string} or \code{choices_selected} with teal module)\cr
name of the variable for the analysis}

\item{avisit}{(\code{string} or \code{choices_selected} with teal module)\cr
value of analysis visit \code{AVISIT} of interest.}

\item{paramcd}{(\code{string} or \code{choices_selected} with teal module)\cr
variable value designating the studied parameter.}

\item{conf_level}{(\code{proportion})\cr confidence level of the interval.
Default is 0.95.}
}
\description{
ANCOVA Teal Module
}
\section{Functions}{
\itemize{
\item \code{template_ancova}: create the expression corresponding to the analysis.
}}

\examples{

library(random.cdisc.data)
library(tern)
adsl <- radsl(cached = TRUE)
adqs <- radqs(cached = TRUE)

# Multiple endpoints at multiple timepoints
a <- teal.modules.clinical:::template_ancova(
  parentname = "adsl",
  dataname = "adqs",
  avisit = c("WEEK 1 DAY 8", "WEEK 2 DAY 15"),
  arm_var = "ARMCD",
  ref_arm = "ARM A",
  cov_var = c("BASE", "STRATA1"),
  aval_var = "CHG",
  paramcd = c("BFIALL", "FATIGI")
)

styled_expr(a$data)
styled_expr(a$layout)
styled_expr(a$table)

b <- mapply(expr = a, FUN = eval)
b$table

# Single endpoint at single timepoint
a <- teal.modules.clinical:::template_ancova(
  parentname = "adsl",
  dataname = "adqs",
  avisit = "WEEK 1 DAY 8",
  arm_var = "ARMCD",
  ref_arm = "ARM A",
  cov_var = c("BASE", "STRATA1"),
  aval_var = "CHG",
  paramcd = "FKSI-FWB"
)

styled_expr(a$data)
styled_expr(a$layout)
styled_expr(a$table)

b <- mapply(expr = a, FUN = eval)
b$table

}
