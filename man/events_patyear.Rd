% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tm_t_events_patyear.R
\name{events_patyear}
\alias{events_patyear}
\alias{template_events_patyear}
\alias{tm_t_events_patyear}
\title{Teal module: Event rates adjusted for patient-years}
\usage{
template_events_patyear(
  dataname,
  parentname,
  arm_var,
  paramcd,
  control = control_incidence_rate(),
  event_indicator = 0,
  add_total = TRUE
)

tm_t_events_patyear(label, dataname, arm_var, paramcd)
}
\arguments{
\item{dataname}{(\code{string})\cr analysis data used in teal module.}

\item{parentname}{(\code{string})\cr parent analysis data used in teal module,
usually this refers to \code{"ADSL"}.}

\item{arm_var}{(\code{string} or \code{choices_selected} with teal module)\cr
variable names designating the study arm.}

\item{paramcd}{(\code{string} or \code{choices_selected} with teal module)\cr
variable value designating the studied parameter.}

\item{control}{(\code{list})\cr list of settings for the analysis.}

\item{event_indicator}{(\code{numeric})\cr code for event as opposed to censoring.}

\item{add_total}{(\code{flag})\cr whether to include column with total number of patients.}

\item{label}{(\code{character})\cr menu item label of the module in the teal app}
}
\description{
Teal module: Event rates adjusted for patient-years
}
\section{Functions}{
\itemize{
\item \code{template_events_patyear}: create the expression corresponding to the analysis.

\item \code{tm_t_events_patyear}: Teal module for incidence rate.
}}

\examples{
# Preparation of the test case.
library(dplyr)
library(random.cdisc.data)
adsl <- radsl(cached = TRUE)
adaette <- radaette(cached = TRUE)
app <- init(
  data = cdisc_data(
    cdisc_dataset("ADSL", adsl),
    cdisc_dataset("ADAETTE", adaette),
    code =
      "ADSL <- radsl(cached = TRUE)
    ADAETTE <- radaette(cached = TRUE)"
  ),
  modules = root_modules(
    tm_t_events_patyear(
      label = "AE rate adjusted for patient-years at risk Table",
      dataname = "ADAETTE",
      arm_var = choices_selected(c("ARM", "ARMCD"), "ARMCD"),
      paramcd = choices_selected(levels(adaette$PARAMCD), "AETTE1")
    )
  )
)

\dontrun{
shinyApp(app$ui, app$server)
}

}
